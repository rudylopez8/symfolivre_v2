{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="mb-3">
    {{ form_label(form.book, 'Livre', {'label_attr': {'class': 'form-label'}}) }}
    {{ form_widget(form.book, {'attr': {'class': 'form-select'}}) }}
    <div class="form-text">Sélectionnez un livre à ajouter à votre panier de lecture.</div>
    {{ form_errors(form.book) }}
</div>

<div class="d-flex gap-2">
    <button type="submit" class="btn btn-primary">
        <i class="fa-solid fa-plus me-1"></i>Ajouter au panier
    </button>
    <a href="{{ path('app_reading_basket_index') }}" class="btn btn-outline-secondary">Annuler</a>
</div>

{{ form_end(form) }}